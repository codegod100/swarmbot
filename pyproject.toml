[project]
name = "swarmbot"
version = "0.1.0"
description = "IRC bot that dispatches @mentions to Letta agents"
requires-python = ">=3.9"
dependencies = [
    "aiohttp>=3.9",
    "pyyaml>=6.0",
]

[project.scripts]
swarmbot = "bot:main"
